import React from "react";

const salesDataHandler = (data) => {
    // total values will be derived here

// console.log(data);
// console.log(data.salesDeductionOverTime.discount.values.reduce((acc, curr) => acc+curr,0));
return data;
}

export default salesDataHandler;
